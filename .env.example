# Copy this file to .env.development / .env.staging / .env.production and fill in values.
# Run `ipconfig` (Windows) or `ifconfig` (Mac/Linux) to get your LAN IP for .env.development.

EXPO_PUBLIC_ENV=development
EXPO_PUBLIC_API_URL=http://192.168.x.x:3000/api/v1
EXPO_PUBLIC_TENANT_SLUG=default
